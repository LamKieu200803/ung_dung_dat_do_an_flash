<!DOCTYPE html>
<html>
<head>
  <title>Sửa Sản Phẩm</title>
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css">
  <script src="https://code.jquery.com/jquery-3.2.1.slim.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.12.9/umd/popper.min.js"></script>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/js/bootstrap.min.js"></script>
</head>
<body>
  <div class="container">
    <h1>Sửa Sản Phẩm</h1>
    <form action="/sanpham/sua/<%= id %>" method="POST">
      <div class="form-group">
        <label for="tensp">Tên sản phẩm:</label>
        <!-- Thêm giá trị mặc định từ biến server -->
        <input type="text" class="form-control" id="tensp" name="tensp" value="<%= product.tensp %>" required>
      </div>
      <div class="form-group">
        <label for="giasp">Giá sản phẩm:</label>
        <!-- Thêm giá trị mặc định từ biến server -->
        <input type="number" class="form-control" id="giasp" name="giasp" value="<%= product.giasp %>" required>
      </div>
      <div class="form-group">
        <label for="img">Ảnh sản phẩm:</label>
        <!-- Thêm giá trị mặc định từ biến server -->
        <input type="text" class="form-control" id="img" name="img" value="<%= product.img %>" required>
      </div>
      <div class="form-group">
        <label for="motasp">Mô tả sản phẩm:</label>
        <!-- Thêm giá trị mặc định từ biến server -->
        <textarea class="form-control" id="motasp" name="motasp" required><%= product.motasp %></textarea>
      </div>
      <div class="form-group">
        <label for="soluong">Số lượng:</label>
        <!-- Thêm giá trị mặc định từ biến server -->
        <input type="number" class="form-control" id="soluong" name="soluong" value="<%= product.soluong %>" required>
      </div>
      <button type="submit" class="btn btn-primary">Sửa</button>
    </form>
  </div>
</body>
</html>
